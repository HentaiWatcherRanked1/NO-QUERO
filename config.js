window.CONVOY_CONFIG = {
  // Create a free Supabase project, then paste these two values.
  // The anon/publishable key is intended for browser clients.
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",

  // Optional Mapbox browser token for routing/navigation.
  MAPBOX_ACCESS_TOKEN: ""
};
